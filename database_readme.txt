To load the json files, first run the application to generate the collections 
even if there are no entries yet. The using Mongo Compass, load the files:

  Load memory.board_cards.json into the board_cards collection
  Load memory.cards.json into the cards collection
